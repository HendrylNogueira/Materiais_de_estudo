#include <stdio.h>

int main()
{
    /* assim utilizamos comentários*/
    // Assim utilizamos comentários para uma unica linha
    char nome[30];
    printf("Digite seu nome:");
    scanf("%s", nome);

    printf("Seu nome e: %s", nome);
    printf(" ");
    return 0;
}